version: 2

models:
  - name: customers
    description: all customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
  - name: orders
    description: all orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
