version: 2

sources:
  - name: ecomm
    database: raw
    tables:
      - name: deliveries
      - name: customers
        description: Each record in customer table represents a customer in ecommerce application
        columns:
          - name: id
            description: "{{ doc('customer_id')}}"
      - name: orders_us
        description: Each record in this table represents an order in ecommerce application

  - name: sheets
    database: raw
    tables:
      - name: customer_survey_responses