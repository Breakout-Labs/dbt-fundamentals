version: 2

sources:
  - name: ecomm
    database: raw
    tables:
      - name: deliveries
      - name: customers
        description: This is the customers.
        columns:
          - name: id
            description: '{{ doc("customer_id") }}'
      - name: orders
        description: This are the orders received from US customers.
        identifier: orders_us
        
  - name: sheets
    database: raw
    tables:
      - name: customer_survey_responses