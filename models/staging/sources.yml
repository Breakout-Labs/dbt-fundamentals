sources:
  - name: banking
    schema: raw_banking
    database: breakout-labs-training
    tables:
      - name: banks
      - name: customers
        description: Customer data from our banking system
        columns:
          - name: id
            description: '{{ doc("customer_id") }}'
      - name: loan_applications
      - name: loan_decisions